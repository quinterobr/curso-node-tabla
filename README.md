#Notas:

```
Options:

      --help     Show help                                            
      [boolean]      --version  Show version number                                  
      [boolean]  -b, --base     La base de la tabla de multiplicar         
      [number] [required]  -l, --listar   Muestra la tabla por consola        
      [boolean] [default: false]  -h, --hasta    Limite de la tabla de multiplicar

```
